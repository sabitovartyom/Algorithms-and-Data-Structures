#include <iostream>
#include <vector>

using namespace std;

int main(){
    int N;
    cin >> N;
    vector<vector<vector<int>>> costs(N, vector<int>(N), vector<vector<int>>(2));
    vector<vector<int>> minTable(N, vector<int>(N));

    
}
